#!/bin/bash
# CLI wrapper for caption optimizer
# Makes it easy to use from anywhere: optimize-caption "your idea"

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
python3 "$SCRIPT_DIR/optimize_caption.py" "$@"
