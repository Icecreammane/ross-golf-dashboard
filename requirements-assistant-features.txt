# Core Assistant Features Requirements
# Install: pip3 install -r requirements-assistant-features.txt

# Financial Dashboard (Plaid Integration)
plaid-python>=14.0.0
flask>=3.0.0

# Reservation Finder (Web Scraping)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Email Triage (Gmail API)
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.100.0

# Optional: Better HTML parsing
lxml>=4.9.0

# Note: Ollama must be installed separately (brew install ollama)
